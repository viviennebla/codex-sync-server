{
  "name": "codex-sync-server",
  "version": "1.0.0",
  "description": "Headless sync server for Codex Usage Dashboard — stores device snapshots, serves pull endpoints",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "license": "MIT"
}
